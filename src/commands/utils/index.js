const arithmetic = require("./arithmetic");
const { autoReroll, matchAutoReroll } = require("./autoReroll");
const hasRole = require("./hasRole");
const replyLongMessage = require("./replyLongMessage");

module.exports = {
  arithmetic,
  autoReroll,
  matchAutoReroll,
  hasRole,
  replyLongMessage,
};
